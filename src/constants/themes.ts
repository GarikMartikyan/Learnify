import type { ThemeMode } from "./types/index.types.ts";
import type { ThemeConfig } from "antd";

export const themes: Record<ThemeMode, ThemeConfig> = {
  light: {
    token: {
      colorPrimary: "#9f98cb",
    },
  },
  dark: {
    token: {
      colorPrimary: "#9f98cb",
    },
  },
};
